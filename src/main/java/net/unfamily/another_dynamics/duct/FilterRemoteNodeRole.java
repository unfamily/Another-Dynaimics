package net.unfamily.another_dynamics.duct;

/** Which logistics path evaluates per-line destination bindings. */
public enum FilterRemoteNodeRole {
    EXTRACT_ROUTE,
    FILTER_INBOUND,
    FILTER_DONOR,
    RETRIEVE_PULL
}
